<h1>Listing livros</h1>

<table>
  <tr>
    <th>Titulo</th>
    <th>Autor</th>
    <th>Editora</th>
    <th>Preco</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @livros.each do |livro| %>
  <tr>
    <td><%= livro.titulo %></td>
    <td><%= livro.autor %></td>
    <td><%= livro.editora %></td>
    <td><%= livro.preco %></td>
    <td><%= link_to 'Show', livro %></td>
    <td><%= link_to 'Edit', edit_livro_path(livro) %></td>
    <td><%= link_to 'Destroy', livro, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Livro', new_livro_path %>
